pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

plugins {
    // Lets Gradle auto-provision the Java 21 toolchain JDK (Gradle itself runs on JDK 17, compiles to 21).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}
